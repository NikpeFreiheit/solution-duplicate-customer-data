TO DO
--DONE 1. Скорректировать container по component диаграмме
     2. Проверить sequense все диаграмы на последовательность и наименования participants 
    (будет изменение на отображение дублированных заявок. на фронт будут тянуться из Postgre из спец.таблицы)
--DONE 3. Добавить в mongo-raw-schema поле src_system и как его определять для разных источников (CRM/API) 
     4. Дописать методы API (!!!--Не забыть про описание Response для каждого метода + errors--!!!)
    --DONE 4.1 ssn-integration-api.yaml
        --Не делала. Избыточно. Select CRM -> Postgre (выполняется select для сбора данных и преобразование в json для отправки на API SSN service)
        --DONE POST CRM -> SSN service
        --DONE Response SSN service -> CRM
        --Не делала. Избыточно. Insert CRM -> Postgre (выполняется insert обогащенных данных)
        4.2 partner-integration-api.yaml
     --DONE POST Partner -> API gateway
            POST API gateway -> MongoDB
            GET Normal ->  MongoDB
            POST Normal -> Valid
            GET Valid -> Postgre
            POST Valid -> Postgre (сохранение валидированных данных, у которых нет дублей python берет json, собирает insert для Postgre)
            POST Valid -> Postgre (сохранение в табл deduplication_queue для проверки на дубли python берет json, собирает insert для Postgre)
        4.3 crm-to-normalization-api.yaml
            POST CRM -> MongoDB
            GET Normal ->  MongoDB
            POST Normal -> Valid
            GET Valid -> Postgre
            POST Valid -> Postgre (сохранение валидированных данных без проверки дублей)
            POST Valid -> Postgre (сохранение в табл deduplication_queue для проверки на дубли)
--DONE 5. Скореектировать ER по новой структуре
--Не делала. Избыточно. 6. Как будут отображаться данные пользователям, чтобы они подтверждали дубли